# Built binary is intentionally TRACKED so Claude Code's marketplace
# install copies the latest build into the plugin cache. Regenerated by
# scripts/install.sh on each build.

# Go build cache / coverage
*.test
*.out
coverage.txt

# Editor / OS noise
.DS_Store
.idea/
.vscode/
